apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./namespace.yaml
  - ./helmrelease.yaml

secretGenerator:
  - name: helm-values-local-path-provisioner
    files:
      - ./values.yaml

configurations:
  - ../../../common/flux-name-ref.yaml
